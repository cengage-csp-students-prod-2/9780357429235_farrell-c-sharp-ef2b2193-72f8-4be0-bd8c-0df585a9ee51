using System;
using static System.Console;
using System.Globalization;
class Comments
{
	static void Main()
	{
		WriteLine(" Two way to write out comments");
		// Let a line becomes comment by adding two slashes
		/* Let a block becomes comment by adding a slash and 
		an asterisk at the beginning and an asterisk and a 
		slash at the end */ 
	}
}
